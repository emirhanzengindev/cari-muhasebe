generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Minimal schema for build purposes
// Actual schema will be defined based on your application needs
model User {
  id    String  @id @default(uuid())
  email String  @unique
  name  String?
}